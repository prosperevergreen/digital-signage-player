import style from './AppLogo.module.scss';

const AppLogo = () => {
  return (
    <h1 className={style.AppLogoComponent}>
      <span className='digital'>Digital</span> <span>Signage Player</span>
    </h1>
  );
};

export default AppLogo;
